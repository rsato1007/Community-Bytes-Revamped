// controllers index.js
// Much like the index file for middleware, models, routes, etc. this file's
// purpose is to act a central hub for controller files. It's
// not necessary, but results in better organization and hopefully better scalable down the road.

// Controllers portion as per the MVC model.
module.exports = {
    // The examples below are for your reference.
    // users: require("./users"),

    // Each file below imports controller functions needed
    // Authentication Controller
    // Users Controller
    // Recipes Controller
    // Ingredients Controller
    // Reviews Controller
};