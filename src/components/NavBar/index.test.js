import NavBar from ".";
import {render} from '@testing-library/react';
import '@testing-library/jest-dom';

// Contains all tests pertaining to NavBar.

test("Placeholder", ()=> {
    expect(2).toBe(2);
})