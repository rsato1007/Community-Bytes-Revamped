import React from "react";

// When the user goes to a URL that doesn't correspond to anything on the website,
// this component will render.
const NotFound = () => {
    return (
        <div>
            Page not found!
        </div>
    )
}

export default NotFound;